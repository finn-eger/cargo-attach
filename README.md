`cargo-attach` runs `probe-rs attach` with the most recently modified binary or
example for the current package, allowing you to view logs without resetting the
microcontroller or waiting to re-upload.
